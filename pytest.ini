[pytest]
testpaths = tests
# `tests/` imports `src.*`, which resolves through the repo root — the same reason
# everything here is run as `venv/bin/python3 -m <tool>` from the project root. The suite
# also needs a Postgres it may truncate; see the header of `tests/conftest.py`.
addopts = -q
