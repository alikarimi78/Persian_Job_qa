[pytest]
testpaths = tests
# `tests/` imports `app.*`, which resolves through the repo root — the same reason
# everything here is run as `venv/bin/python3 -m <tool>` from the project root.
addopts = -q
